{
  "name": "package-size-badge-action",
  "version": "1.0.0",
  "description": "GitHub Action: measure npm package bundle size (min+gzip/brotli), cache per version, publish SVG badges",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist -m --target es2022 --external esbuild && printf '{\\n  \"type\": \"commonjs\"\\n}\\n' > dist/package.json",
    "typecheck": "tsc --noEmit",
    "measure": "tsx src/cli.ts",
    "test": "npm run typecheck && npm run build && tsx test/smoke.mts",
    "test:real": "npm run build && tsx test/smoke-header-map.mts",
    "test:all": "npm test && npm run test:real"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.1",
    "esbuild": "^0.25.9"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "@vercel/ncc": "^0.38.3",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  },
  "license": "MIT"
}
